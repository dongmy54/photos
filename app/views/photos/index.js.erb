$(".photos-list .col-md-4").remove();    

<% if @photos.present? %>
  <%= render 'photos_present' %>
<% else %>
  $(".photos-list #title").html('<%= @current_tag %>下暂无图片');
  $(".pagination").remove();
<% end %>

$('.pagination a').attr('data-remote', 'true');  /* 注意添加ajax的位置 */          